# Template Bucket

Put reusable templates in this folder.

- `playbook/` stores skill-extension guides and standards.
- `playbook/tpl/` stores reusable template artifacts used during skill execution.
- Discovery templates/logs are process docs and live under `docs/discovery/`.
